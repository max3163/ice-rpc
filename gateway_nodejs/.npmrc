# Pin the public registry. Without this, a developer machine configured for a
# corporate mirror rewrites the `resolved` URLs of package-lock.json; those
# pinned URLs then break `npm ci` on any network that cannot reach the mirror
# (a GitHub-hosted CI runner, for instance).
registry=https://registry.npmjs.org/
