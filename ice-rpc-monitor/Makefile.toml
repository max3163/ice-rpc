# Makefile.toml — Extension of the workspace Makefile for `ice-rpc-monitor`.
#
# `cargo make monitoring-demo-detail` etc. are defined in the root Makefile.toml;
# this file only makes them resolvable when cargo-make is run from this crate.
extend = "../Makefile.toml"
